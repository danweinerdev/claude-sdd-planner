{
  "type": "phase",
  "additionalSections": "allowed",
  "frontmatterMode": "preserve",
  "frontmatter": [
    {
      "key": "title",
      "ownership": "author",
      "required": true
    },
    {
      "key": "type",
      "ownership": "tool",
      "required": true,
      "fixed": "phase"
    },
    {
      "key": "status",
      "ownership": "tool",
      "required": true,
      "enum": [
        "planned",
        "in-progress",
        "complete",
        "blocked",
        "deferred"
      ]
    },
    {
      "key": "created",
      "ownership": "tool",
      "required": true
    },
    {
      "key": "updated",
      "ownership": "tool",
      "required": true
    },
    {
      "key": "plan",
      "ownership": "author",
      "required": true
    },
    {
      "key": "phase",
      "ownership": "author",
      "required": true
    },
    {
      "key": "deliverable",
      "ownership": "author",
      "required": true
    },
    {
      "key": "tags",
      "ownership": "author",
      "required": false,
      "default": "[]"
    },
    {
      "key": "related",
      "ownership": "author",
      "required": false,
      "default": "[]"
    },
    {
      "key": "waivers",
      "ownership": "author",
      "required": false,
      "doc": "Accepted exceptions: entries of {code, reason, accepted}. A listed finding is still reported, as WAIVED with its reason, but stops making the root invalid. Waiving a parse-stage code is refused, an unexplained waiver is refused (SDD176), and a waiver matching nothing is reported as stale (SDD177).",
      "default": "[]"
    }
  ],
  "headings": [
    {
      "text": "## Overview",
      "depth": 2,
      "required": true,
      "defaultBody": "TODO: section inserted by `sdd migrate`; content required by the schema."
    },
    {
      "text": "## Acceptance Criteria",
      "depth": 2,
      "required": true,
      "defaultBody": "TODO: section inserted by `sdd migrate`; content required by the schema."
    },
    {
      "text": "## Phase Completion Evidence",
      "depth": 2,
      "required": true,
      "defaultBody": "Pending \u2014 not complete."
    }
  ],
  "namespaces": []
}
