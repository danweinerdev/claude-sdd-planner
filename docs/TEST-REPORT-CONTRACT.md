# Test report contract

The installed contract is available from `sdd graph evidence-contract`.
The shipped Markdown copy is [`shared/test-evidence-contract.md`](../shared/test-evidence-contract.md); its canonical source is embedded in the `sdd` binary.
