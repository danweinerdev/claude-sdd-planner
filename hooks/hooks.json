{
  "_comment": "Bootstrap only. `sdd provision` (run by /setup) REGENERATES this file for the platform it runs on, replacing this content with a single command per event. Until then this POSIX form serves macOS and Linux; on Windows the hooks are inert until /setup runs, which is the same state as an unprovisioned install.",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "sh \"${CLAUDE_PLUGIN_ROOT}/hooks/sdd-hook.sh\" sessionstart",
            "timeout": 10,
            "statusMessage": "Loading decision ledger..."
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Bash|Write|Edit|NotebookEdit",
        "hooks": [
          {
            "type": "command",
            "command": "sh \"${CLAUDE_PLUGIN_ROOT}/hooks/sdd-hook.sh\" pretooluse",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
